# _answers_file: ".copier-answers.yml"

# _min_copier_version: '9'
# _templates_suffix: .jinja
# _subdirectory: template
# _overwrite: true

# # Project info
# project:
#   type: str
#   help: "Enter data science project name (e.g., blk-customer)"

# model:
#   type: yaml
#   default:
#     - name: risk_level_model


# _force:
#   - tasks.yml

# overwrite: true
# update: true

_answers_file: ".copier-answers.yml"
_min_copier_version: '9'
_templates_suffix: .jinja
_subdirectory: template

# Project info
project:
  type: str
  help: "Enter data science project name (e.g., blk-customer)"

model:
  type: yaml
  default:
    - name: risk_level_model

_skip_if_exists:
  - tasks.yml
  - "!models.yml"  # Always overwrite this file